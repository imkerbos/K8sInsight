# 数据库连接（指向你单独维护的 PostgreSQL）
DB_HOST=host.docker.internal
DB_PORT=5432
DB_USER=k8sinsight
DB_PASSWORD=changeme
DB_DBNAME=k8sinsight

# 认证配置
JWT_SECRET=change-me-in-production
DEFAULT_ADMIN_PASSWORD=k8sinsight

# 服务端口
BACKEND_PORT=18080
FRONTEND_PORT=3010
